## IDF Component Manager Manifest File
dependencies:
  espressif/esp-zboss-lib: "^1.2.3"
  espressif/esp-zigbee-lib: "^1.2.3"
  espressif/button: "^3.2.0"
  ## Required IDF version
  idf:
    version: ">=4.1.0"
  ieee802154:
    path: ${MININO_PATH}/components/ieee802154
  openthread:
    path: ${MININO_PATH}/components/openthread
  OTA:
    path: ${MININO_PATH}/components/OTA
  ble_hid:
    path: ${MININO_PATH}/components/ble_hid
  espressif/iperf:
    version: "~0.1.1"
  # badge_connect:
    # git: https://github.com/ElectronicCats/badge_connect_bin
    # version: "1.0.0"

  # # Put list of dependencies here
  # # For components maintained by Espressif:
  # component: "~1.0.0"
  # # For 3rd party components:
  # username/component: ">=1.0.0,<2.0.0"
  # username2/component2:
  #   version: "~1.0.0"
  #   # For transient dependencies `public` flag can be set.
  #   # `public` flag doesn't have an effect dependencies of the `main` component.
  #   # All dependencies of `main` are public by default.
  #   public: true
